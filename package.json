{
  "name": "akems-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @akems/api start:prod",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "verify-author": "node scripts/verify-provenance.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
